spring:
  profiles:
    group:
      console:
        - app
        - mock-mail
      real:
        - app
        - mail
      db:
        - app
      message-queue:
        - app
        - rabbitmq
      mock-mail:
        - app
        - mock-mail
    active: console
server:
  port: 8081
