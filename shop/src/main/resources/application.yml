spring:
  profiles:
    group:
      console:
        - app
      real:
        - app
        - mail
      db:
        - app
      message-queue:
        - app
        - rabbitmq
    active: console
server:
  port: 8081
